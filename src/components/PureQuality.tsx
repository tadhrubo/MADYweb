import { motion } from 'framer-motion';import { Section } from './Section';import { ingredients } from '../content/ingredients'
export function PureQuality(){return <Section bg="var(--bg)" className="pure-quality"><div className="quality-inner"><motion.p className="quality-eye" initial={{scale:0}} whileInView={{scale:1}} viewport={{once:true}}>PURE QUALITY</motion.p><div className="quality-title">{['EVERY LAYER','PACKED WITH','SIGNATURE','FLAVOR'].map((x,i)=><motion.h2 key={x} initial={{y:80,opacity:0}} whileInView={{y:0,opacity:1}} viewport={{once:true}} transition={{delay:i*.09}}>{x}</motion.h2>)}</div>{ingredients.map((x,i)=><motion.div key={x.name} className="ingredient" style={{left:x.left+'%',top:x.top+'%',width:x.width+'%',rotate:x.rotate}} initial={{x:i<4?-140:140,opacity:0}} whileInView={{x:0,opacity:1}} viewport={{once:true}} transition={{type:'spring',delay:i*.08}}><span>{String(x.name).replace('-',' ')}</span></motion.div>)}</div></Section>}
