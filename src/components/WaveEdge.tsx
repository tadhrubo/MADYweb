import { useEffect, useRef, useState } from 'react'
export function WaveEdge({fill}:{fill:string}){const path=useRef<SVGPathElement>(null);const svg=useRef<SVGSVGElement>(null);const [visible,setVisible]=useState(true);useEffect(()=>{const node=svg.current;if(!node)return;const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;const io=new IntersectionObserver(([entry])=>setVisible(entry.isIntersecting));io.observe(node);let frame=0,last=0,start=performance.now();const draw=(now:number)=>{if(!reduced&&visible&&now-last>33){last=now;const t=(now-start)/1000;let d='M 0 92 ';for(let x=0;x<=1000;x+=8){const y=44+Math.sin(x/550*2*Math.PI+t*.18)*22+Math.sin(x/300*2*Math.PI-t*.11)*10;d+=`L ${x} ${y} `}path.current?.setAttribute('d',d+'L 1000 100 L 0 100 Z')}frame=requestAnimationFrame(draw)};draw(performance.now());return()=>{cancelAnimationFrame(frame);io.disconnect()}},[visible]);return <svg ref={svg} className="wave-edge" viewBox="0 0 1000 100" preserveAspectRatio="none" aria-hidden="true"><path ref={path} fill={fill} d="M 0 44 L 1000 44 L 1000 100 L 0 100 Z"/></svg>}
